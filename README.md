# student-records
simple Express.js application that manages student records. It installs the required npm packages, creates routes for adding students and retrieving students, and uses async/await to simulate asynchronous database operations.
